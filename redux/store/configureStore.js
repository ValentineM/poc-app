import { createStore } from 'redux';
import submitQuestion from '/Users/valentinemichaud/Documents/React-Native/rooted/redux/reducers/questionsReducer';

const store = createStore(submitQuestion);

export default store;